# frozen_string_literal: true

RSpec.describe Minimal::Mistakes::Plus do
  it "has a version number" do
    expect(Minimal::Mistakes::Plus::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(true).to eq(true)
  end
end
